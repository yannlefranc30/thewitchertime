public interface thewitcher {
    public static void main( String[] args ) {

        //je me permet de récupérer le temps.

        // petit bout de code pour m'excercer
        // Récupération du temps en milliseconde
        long timestampMS = System.currentTimeMillis();

        // On affiche le timestamp.
        System.out.printf( "Le temps avec le sorceleur : %d\n", timestampMS );

    }

}